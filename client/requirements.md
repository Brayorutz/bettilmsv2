## Packages
(none needed)

## Notes
Fonts are imported via client/src/index.css (Google Fonts).
Static logo image: import from @assets/logo1_1770205562697.jpg (do not reference as a URL string).
Auth: Use /api/login and /api/logout (no custom login forms). Use /api/me to resolve { user, role }.
All API calls use credentials: "include".
